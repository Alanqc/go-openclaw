# Binary (root only, not cmd/openclaw-go/)
/openclaw-go
*.exe

# Config with secrets (keep example)
openclaw.yaml

# Local secrets file (never commit; use goopenclaw.secrets.example as template)
goopenclaw.secrets
.openclaw/goopenclaw.secrets

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
